apiVersion: v1
kind: Service
metadata:
  name: ml-anomaly-detector
  namespace: ai-monitoring
  labels:
    app: ml-anomaly-detector
spec:
  type: ClusterIP
  ports:
    - port: 8001
      targetPort: 8001
      protocol: TCP
      name: http
  selector:
    app: ml-anomaly-detector
