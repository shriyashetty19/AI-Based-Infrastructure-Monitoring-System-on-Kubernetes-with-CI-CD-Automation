apiVersion: v1
kind: Service
metadata:
  name: demo-service
  namespace: ai-monitoring
  labels:
    app: demo-service
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: demo-service
